extends layout

block content
  nav
   ul
    li
     a(href='getAllHeros') GET MORE HEROS
  h1= title
  
 
  
  form(action='saveData' method='get')
   .row
    .input-field.col.s6
     label(for='name') Name
     input#name.validate(type='text',name='name')
   .row
    .input-field.col.s6
     label(for='phoneNo') Phone No
     input#phoneNo.validate(type='text',name='phoneNo')
   
   
   .row
    button.btn(type='submit') Submit
